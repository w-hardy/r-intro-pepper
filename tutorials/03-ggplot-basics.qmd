---
title: "DRAFT: Visualisation with ggplot2"
---

We will plot the grouped means from the previous tutorial.

```{r}
#| message: false
library(dplyr) # Data manipulation
library(ggplot2) # Figures

penguins |> 
  ggplot(aes(x = species)) +
  geom_boxplot(aes(y = bill_len))
```